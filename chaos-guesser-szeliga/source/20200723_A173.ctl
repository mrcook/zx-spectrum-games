; 23rd July 2020

C $A173 Load address of 4th map table which contains owner numbers + other stuff
C $A176 Set loop counter to cover all bytes in the table.
C $A178 Reset bit 7 for the current byte in the table.
C $A17A Point at the next byte.
C $A17B Loop back.
C $A17D Return.
